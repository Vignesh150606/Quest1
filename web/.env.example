# Base URL of the deployed FastAPI backend (Render), no trailing slash.
# Local dev default (matches `uvicorn api.app:app --port 8000`): http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
